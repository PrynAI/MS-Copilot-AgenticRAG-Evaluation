# From your project root
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1

# Core libraries
pip install -U langgraph langchain-core langchain-openai

# Azure AI Search SDK
pip install -U azure-search-documents

# SQL client stack
pip install -U pyodbc sqlalchemy

# Config + utils
pip install -U pydantic-settings python-dotenv tenacity


Install ODBC Driver 18 for SQL Server

